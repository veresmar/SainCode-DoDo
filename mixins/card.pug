mixin card(imageUrl, title, content, price, buttonText)
  .card
    img(src=imageUrl)
    //- .card-mobile
    //-   .card-text
    .card-mobile
      h3= title
      p.card-text-content= content
      .card-price 
        p.card-price-p 
          =price
        +button-S(buttonText)
    
